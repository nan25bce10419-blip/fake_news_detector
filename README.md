# fake_news_detector
This project detects fake news using Machine Learning. It uses a labeled dataset of real and fake news articles, applies TF-IDF for text feature extraction, and trains a Naive Bayes classifier to predict authenticity. The system runs through a command-line interface and demonstrates practical NLP and classification techniques.
